# 💸 Transactions API

## ภาพรวม

บันทึกและจัดการธุรกรรมทางการเงินพร้อมความสามารถในการกรองและค้นหาขั้นสูง

**Base Path**: `/protected/api/v1/transactions`

**การยืนยันตัวตน**: จำเป็น (JWT Bearer Token)

## Endpoints

### สร้างธุรกรรม

**POST** `/protected/api/v1/transactions`

**Request Body**:

```json
{
  "category_id": 1,
  "type": "expense",
  "amount": 150.5,
  "date": "2024-03-15",
  "note": "อาหารเที่ยงที่ร้านอาหาร"
}
```

### ดูธุรกรรม (มีตัวกรอง)

**GET** `/protected/api/v1/transactions`

**Query Parameters**:

- `page` (ไม่บังคับ): หมายเลขหน้าสำหรับแบ่งหน้า
- `limit` (ไม่บังคับ): จำนวนรายการต่อหน้า
- `type` (ไม่บังคับ): "income" หรือ "expense"
- `category_id` (ไม่บังคับ): กรองตามหมวดหมู่
- `start_date` (ไม่บังคับ): วันที่เริ่มต้น (YYYY-MM-DD)
- `end_date` (ไม่บังคับ): วันที่สิ้นสุด (YYYY-MM-DD)

### ดูธุรกรรมตาม ID

**GET** `/protected/api/v1/transactions/:id`

### แก้ไขธุรกรรม

**PUT** `/protected/api/v1/transactions/:id`

**Request Body** (แก้ไขบางส่วน):

```json
{
  "amount": 200.0,
  "note": "อัปเดตค่าอาหารเที่ยง"
}
```

### ลบธุรกรรม

**DELETE** `/protected/api/v1/transactions/:id`

## รูปแบบ Response

```json
{
  "success": true,
  "message": "ดึงธุรกรรมสำเร็จ",
  "data": [
    {
      "transaction_id": 1,
      "category_id": 1,
      "category_name": "อาหาร",
      "type": "expense",
      "amount": 150.5,
      "date": "2024-03-15",
      "note": "อาหารเที่ยง",
      "created_date": "2024-03-15T10:30:00Z"
    }
  ],
  "pagination": {
    "current_page": 1,
    "total_pages": 5,
    "total_items": 48
  }
}
```

## ตัวอย่างการใช้งาน

### สร้างธุรกรรมรายจ่าย

```bash
curl -X POST "http://localhost:3000/protected/api/v1/transactions" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "category_id": 1,
    "type": "expense",
    "amount": 150.50,
    "date": "2024-03-15",
    "note": "อาหารเที่ยง"
  }'
```

### กรองธุรกรรมตามช่วงวันที่

```bash
curl -X GET "http://localhost:3000/protected/api/v1/transactions?start_date=2024-03-01&end_date=2024-03-31" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

---

[← กลับไปยังเอกสาร API](../README.md)
