# 📈 Reports & Analytics API

## ภาพรวม

Endpoints สำหรับรายงานและการวิเคราะห์ทางการเงินแบบครบครันสำหรับผู้ใช้ Budget Buddy

**Base Path**: `/protected/api/v1/reports`

**การยืนยันตัวตน**: จำเป็น (JWT Bearer Token)

## Endpoints

### 1. สรุปการเงิน

**GET** `/protected/api/v1/reports/summary`

**คำอธิบาย**: ดูสรุปการเงินรายเดือน รวมรายรับ รายจ่าย และยอดคงเหลือ

**Query Parameters**:

- `month` (ไม่บังคับ): รูปแบบ YYYY-MM เช่น "2024-03"
- `user_id` (ไม่บังคับ): สำหรับผู้ดูแลระบบเพื่อดูข้อมูลผู้ใช้คนอื่น

**Response**:

```json
{
  "success": true,
  "message": "ดึงรายงานสรุปสำเร็จ",
  "data": {
    "total_income": 50000,
    "total_expense": 35000,
    "balance": 15000,
    "as_of": "2024-03"
  }
}
```

### 2. ธุรกรรมล่าสุด

**GET** `/protected/api/v1/reports/recent-transactions`

**คำอธิบาย**: ดูรายการธุรกรรมล่าสุดพร้อมรายละเอียด

**Query Parameters**:

- `limit` (ไม่บังคับ): จำนวนธุรกรรมที่ต้องการ (ค่าเริ่มต้น: 10)
- `user_id` (ไม่บังคับ): สำหรับผู้ดูแลระบบ

**Response**:

```json
{
  "success": true,
  "message": "ดึงรายงานธุรกรรมล่าสุดสำเร็จ",
  "data": [
    {
      "transaction_id": 1,
      "category_name": "อาหาร",
      "type": "expense",
      "amount": 150,
      "date": "2024-03-15",
      "note": "อาหารเที่ยง"
    }
  ]
}
```

### 3. เปรียบเทียบรายรับ-รายจ่าย

**GET** `/protected/api/v1/reports/income-vs-expense`

**คำอธิบาย**: การวิเคราะห์เปรียบเทียบรายรับกับรายจ่าย

**Query Parameters**:

- `period` (ไม่บังคับ): "year" หรือ "month"
- `year` (ไม่บังคับ): ปี เช่น "2024"
- `month` (ไม่บังคับ): เดือนในรูปแบบ YYYY-MM
- `user_id` (ไม่บังคับ): สำหรับผู้ดูแลระบบ

**Response**:

```json
{
  "success": true,
  "message": "ดึงรายงานเปรียบเทียบรายรับ-รายจ่ายสำเร็จ",
  "data": [
    {
      "label": "Income",
      "income": 50000,
      "expense": 35000
    }
  ]
}
```

### 4. รายจ่ายตามหมวดหมู่

**GET** `/protected/api/v1/reports/expenses-by-category`

**คำอธิบาย**: แบ่งแยกรายจ่ายตามหมวดหมู่พร้อมเปอร์เซ็นต์

**Query Parameters**:

- `month` (ไม่บังคับ): รูปแบบ YYYY-MM
- `user_id` (ไม่บังคับ): สำหรับผู้ดูแลระบบ

**Response**:

```json
{
  "success": true,
  "message": "ดึงรายงานรายจ่ายตามหมวดหมู่สำเร็จ",
  "data": [
    {
      "category_id": 1,
      "category_name": "อาหาร",
      "amount": 8000,
      "percent": 23
    },
    {
      "category_id": 2,
      "category_name": "คมนาคม",
      "amount": 5000,
      "percent": 14
    }
  ]
}
```

### 5. รายงานปิดเดือน

**GET** `/protected/api/v1/reports/monthly-close`

**คำอธิบาย**: สรุปการเงินรายเดือนแบบสมบูรณ์สำหรับการปิดเดือน

**Query Parameters**:

- `month` (ไม่บังคับ): รูปแบบ YYYY-MM
- `user_id` (ไม่บังคับ): สำหรับผู้ดูแลระบบ

**Response**:

```json
{
  "success": true,
  "message": "ดึงรายงานปิดเดือนสำเร็จ",
  "data": {
    "month": "2024-03",
    "total_income": 50000,
    "total_expense": 35000,
    "net_balance": 15000
  }
}
```

## คุณสมบัติสำหรับผู้ดูแลระบบ

ผู้ดูแลระบบสามารถเข้าถึงรายงานของผู้ใช้คนอื่นได้โดยใส่ parameter `user_id`:

```bash
GET /protected/api/v1/reports/summary?user_id=123&month=2024-03
```

## ตัวอย่างการใช้งาน

### ดูสรุปการเงินเดือนปัจจุบัน

```bash
curl -X GET "http://localhost:3000/protected/api/v1/reports/summary" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### ดูสรุปการเงินเดือนที่กำหนด

```bash
curl -X GET "http://localhost:3000/protected/api/v1/reports/summary?month=2024-03" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### ดูธุรกรรมล่าสุด (จำกัดจำนวน)

```bash
curl -X GET "http://localhost:3000/protected/api/v1/reports/recent-transactions?limit=5" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### การเข้าถึงข้อมูลผู้ใช้ (Admin)

```bash
curl -X GET "http://localhost:3000/protected/api/v1/reports/summary?user_id=123" \
  -H "Authorization: Bearer ADMIN_JWT_TOKEN"
```

## Error Responses

### 400 Bad Request - รูปแบบเดือนไม่ถูกต้อง

```json
{
  "success": false,
  "message": "รูปแบบเดือนไม่ถูกต้อง ต้องเป็น YYYY-MM",
  "data": null
}
```

### 401 Unauthorized

```json
{
  "success": false,
  "message": "จำเป็นต้องมี Authorization token",
  "data": null
}
```

### 500 Internal Server Error

```json
{
  "success": false,
  "message": "ไม่สามารถดึงรายงานสรุปได้",
  "data": null
}
```

## หมายเหตุ

- Parameter วันที่ทั้งหมดต้องอยู่ในรูปแบบ YYYY-MM
- การเข้าถึงแบบ Admin ต้องมีการตรวจสอบ role ที่เหมาะสม (จะดำเนินการในอนาคต)
- ระยะเวลาเริ่มต้นคือเดือนปัจจุบันหากไม่ระบุ
- ค่าเงินทั้งหมดเป็นสกุลเงินหลักของผู้ใช้

---

[← กลับไปยังเอกสาร API](../README.md)
