# 📈 Reports & Analytics API

## ภาพรวม

Endpoints สำหรับรายงานและการวิเคราะห์ทางการเงินแบบครบครันสำหรับผู้ใช้ Budget Buddy พร้อมการรองรับแดชบอร์ดที่ปรับปรุงใหม่

**Base Path**: `/protected/api/v1/reports`

**การยืนยันตัวตน**: จำเป็น (JWT Bearer Token)

## Endpoints

### 1. สรุปแดชบอร์ด (ใหม่!)

**GET** `/protected/api/v1/reports/dashboard`

**คำอธิบาย**: ดึงการ์ดแดชบอร์ดสำหรับหน้าหลัก รวมการ์ดรายรับ รายจ่าย และยอดคงเหลือ

**Headers**:

```
Authorization: Bearer {jwt_token}
Content-Type: application/json
```

**Query Parameters**:

| Parameter | Type   | Required  | คำอธิบาย                                  | ตัวอย่าง      |
| --------- | ------ | --------- | ----------------------------------------- | ------------- |
| `month`   | string | ไม่บังคับ | รูปแบบ YYYY-MM สำหรับเดือนที่ต้องการ      | "2024-03"     |
| `user_id` | string | ไม่บังคับ | UUID สำหรับ admin เข้าถึงข้อมูลผู้ใช้อื่น | "uuid-string" |

**Success Response (200)**:

```json
{
  "success": true,
  "message": "ดึงสรุปแดชบอร์ดสำเร็จ",
  "data": {
    "cards": [
      {
        "type": "income",
        "title": "รายรับ",
        "amount": 8900,
        "formatted_amount": "8,900 B",
        "color": "#10B981"
      },
      {
        "type": "expense",
        "title": "รายจ่าย",
        "amount": 2000,
        "formatted_amount": "2,000 B",
        "color": "#EF4444"
      },
      {
        "type": "balance",
        "title": "ยอดคงเหลือ",
        "amount": 6900,
        "formatted_amount": "6,900 B",
        "color": "#3B82F6"
      }
    ],
    "summary": {
      "total_income": 8900,
      "total_expense": 2000,
      "net_balance": 6900,
      "as_of": "2024-03"
    }
  }
}
```

**Error Responses**:

**400 Bad Request**:

```json
{
  "success": false,
  "message": "รูปแบบเดือนไม่ถูกต้อง ต้องเป็น YYYY-MM",
  "error": "INVALID_DATE_FORMAT"
}
```

**401 Unauthorized**:

```json
{
  "success": false,
  "message": "ต้องการการยืนยันตัวตน",
  "error": "UNAUTHORIZED"
}
```

**500 Internal Server Error**:

```json
{
  "success": false,
  "message": "เกิดข้อผิดพลาดภายในเซิร์ฟเวอร์",
  "error": "INTERNAL_ERROR"
}
```

### 2. สรุปการเงินที่ปรับปรุงใหม่

### 4. สรุปการเงินที่ปรับปรุงใหม่

**GET** `/protected/api/v1/reports/enhanced-financial-summary`

**คำอธิบาย**: รายงานสรุปการเงินรายเดือนที่ครอบคลุมพร้อมการเปรียบเทียบ

**Headers**:

```
Authorization: Bearer {jwt_token}
Content-Type: application/json
```

**Query Parameters**:

| Parameter | Type   | Required  | คำอธิบาย                                                   | ตัวอย่าง      |
| --------- | ------ | --------- | ---------------------------------------------------------- | ------------- |
| `year`    | number | ไม่บังคับ | ปีที่ต้องการดูข้อมูล (ค่าเริ่มต้น: ปีปัจจุบัน)             | 2024          |
| `month`   | number | ไม่บังคับ | เดือนที่ต้องการดูข้อมูล (1-12, ค่าเริ่มต้น: เดือนปัจจุบัน) | 3             |
| `user_id` | string | ไม่บังคับ | UUID สำหรับ admin เข้าถึงข้อมูลผู้ใช้อื่น                  | "uuid-string" |

**Success Response (200)**:

```json
{
  "success": true,
  "message": "ดึงสรุปการเงินที่ปรับปรุงใหม่สำเร็จ",
  "data": {
    "overview": {
      "period": "มีนาคม 2024",
      "total_income": 50000,
      "total_expense": 35000,
      "net_income": 15000,
      "savings_rate": 30.0
    },
    "comparison": {
      "previous_month": {
        "income_change": 5.2,
        "expense_change": -2.1,
        "savings_change": 12.5
      }
    },
    "breakdown": {
      "income_categories": [
        {
          "category": "เงินเดือน",
          "amount": 45000,
          "percentage": 90.0
        }
      ],
      "expense_categories": [
        {
          "category": "อาหาร",
          "amount": 15000,
          "percentage": 42.9
        }
      ]
    }
  }
}
```

**Error Responses**:

**400 Bad Request**:

```json
{
  "success": false,
  "message": "เดือนหรือปีไม่ถูกต้อง",
  "error": "INVALID_DATE_PARAMS"
}
```

**401 Unauthorized**:

```json
{
  "success": false,
  "message": "ต้องการการยืนยันตัวตน",
  "error": "UNAUTHORIZED"
}
```

### 3. ธุรกรรมล่าสุดที่ปรับปรุงใหม่

### 3. ธุรกรรมล่าสุดที่ปรับปรุงใหม่

**GET** `/protected/api/v1/reports/recent-transactions`

**คำอธิบาย**: ดูรายการธุรกรรมล่าสุดแบบแบ่งหน้าพร้อมการจัดรูปแบบที่ปรับปรุงใหม่

**Headers**:

```
Authorization: Bearer {jwt_token}
Content-Type: application/json
```

**Query Parameters**:

| Parameter | Type   | Required  | คำอธิบาย                                      | ตัวอย่าง      |
| --------- | ------ | --------- | --------------------------------------------- | ------------- |
| `limit`   | number | ไม่บังคับ | จำนวนธุรกรรม (ค่าเริ่มต้น: 10, สูงสุด: 100)   | 10            |
| `page`    | number | ไม่บังคับ | หมายเลขหน้าสำหรับการแบ่งหน้า (ค่าเริ่มต้น: 1) | 1             |
| `user_id` | string | ไม่บังคับ | UUID สำหรับ admin เข้าถึงข้อมูลผู้ใช้อื่น     | "uuid-string" |

**Success Response (200)**:

```json
{
  "success": true,
  "message": "ดึงรายงานธุรกรรมล่าสุดสำเร็จ",
  "data": {
    "transactions": [
      {
        "transaction_id": 1,
        "category_name": "อาหาร",
        "category_note": "อาหารกลางวันที่ร้านอาหาร",
        "type": "expense",
        "amount": 350,
        "formatted_amount": "-350 บาท",
        "amount_color": "#EF4444",
        "date": "2024-03-15",
        "formatted_date": "15 มี.ค. 2024",
        "note": "อาหารกลางวันที่ร้านอาหาร"
      }
    ],
    "pagination": {
      "current_page": 1,
      "total_pages": 5,
      "total_count": 50,
      "limit": 10,
      "has_next": true,
      "has_previous": false
    },
    "summary": {
      "showing": 10,
      "total": 50
    }
  }
}
```

**Error Responses**:

**400 Bad Request**:

```json
{
  "success": false,
  "message": "พารามิเตอร์การแบ่งหน้าไม่ถูกต้อง",
  "error": "INVALID_PAGINATION"
}
```

**401 Unauthorized**:

```json
{
  "success": false,
  "message": "ต้องการการยืนยันตัวตน",
  "error": "UNAUTHORIZED"
}
```

      "amount": 150,
      "date": "2024-03-15",
      "note": "อาหารเที่ยง"
    }

]
}

```

### 3. เปรียบเทียบรายรับ-รายจ่าย

### 5. รายได้เทียบกับค่าใช้จ่าย

**GET** `/protected/api/v1/reports/income-vs-expense`

**คำอธิบาย**: รายงานเปรียบเทียบรายได้และค่าใช้จ่ายรายเดือนสำหรับการสร้างกราฟ

**Headers**:
```

Authorization: Bearer {jwt_token}
Content-Type: application/json

````

**Query Parameters**:

| Parameter | Type | Required | คำอธิบาย | ตัวอย่าง |
|-----------|------|----------|---------|---------|
| `year` | number | ไม่บังคับ | ปีที่ต้องการดูข้อมูล (ค่าเริ่มต้น: ปีปัจจุบัน) | 2024 |
| `months` | number | ไม่บังคับ | จำนวนเดือนย้อนหลัง (ค่าเริ่มต้น: 6, สูงสุด: 12) | 6 |
| `user_id` | string | ไม่บังคับ | UUID สำหรับ admin เข้าถึงข้อมูลผู้ใช้อื่น | "uuid-string" |

**Success Response (200)**:

```json
{
  "success": true,
  "message": "ดึงรายงานรายได้เทียบกับค่าใช้จ่ายสำเร็จ",
  "data": {
    "period": "6 เดือนล่าสุด",
    "chart_data": [
      {
        "month": "ต.ค. 2023",
        "income": 48000,
        "expense": 32000,
        "net": 16000
      },
      {
        "month": "พ.ย. 2023",
        "income": 50000,
        "expense": 35000,
        "net": 15000
      },
      {
        "month": "ธ.ค. 2023",
        "income": 52000,
        "expense": 38000,
        "net": 14000
      }
    ],
    "summary": {
      "avg_income": 50000,
      "avg_expense": 35000,
      "avg_savings": 15000,
      "best_month": "ต.ค. 2023",
      "worst_month": "ธ.ค. 2023"
    }
  }
}
````

**Error Responses**:

**400 Bad Request**:

```json
{
  "success": false,
  "message": "จำนวนเดือนต้องอยู่ระหว่าง 1-12",
  "error": "INVALID_MONTHS_PARAM"
}
```

**401 Unauthorized**:

```json
{
  "success": false,
  "message": "ต้องการการยืนยันตัวตน",
  "error": "UNAUTHORIZED"
}
```

### 4. รายจ่ายตามหมวดหมู่

### 6. ค่าใช้จ่ายตามหมวดหมู่

**GET** `/protected/api/v1/reports/expenses-by-category`

**คำอธิบาย**: รายงานค่าใช้จ่ายแยกตามหมวดหมู่สำหรับการแสดงผลเป็นกราฟวงกลม

**Headers**:

```
Authorization: Bearer {jwt_token}
Content-Type: application/json
```

**Query Parameters**:

| Parameter    | Type   | Required  | คำอธิบาย                                                   | ตัวอย่าง      |
| ------------ | ------ | --------- | ---------------------------------------------------------- | ------------- |
| `year`       | number | ไม่บังคับ | ปีที่ต้องการดูข้อมูล (ค่าเริ่มต้น: ปีปัจจุบัน)             | 2024          |
| `month`      | number | ไม่บังคับ | เดือนที่ต้องการดูข้อมูล (1-12, ค่าเริ่มต้น: เดือนปัจจุบัน) | 3             |
| `min_amount` | number | ไม่บังคับ | จำนวนเงินขั้นต่ำที่จะแสดง (ค่าเริ่มต้น: 0)                 | 100           |
| `user_id`    | string | ไม่บังคับ | UUID สำหรับ admin เข้าถึงข้อมูลผู้ใช้อื่น                  | "uuid-string" |

**Success Response (200)**:

```json
{
  "success": true,
  "message": "ดึงรายงานค่าใช้จ่ายตามหมวดหมู่สำเร็จ",
  "data": {
    "period": "มีนาคม 2024",
    "total_expense": 35000,
    "categories": [
      {
        "category_id": 1,
        "category_name": "อาหาร",
        "amount": 15000,
        "percentage": 42.9,
        "color": "#FF6B6B",
        "transaction_count": 45
      },
      {
        "category_id": 2,
        "category_name": "ค่าเช่าบ้าน",
        "amount": 12000,
        "percentage": 34.3,
        "color": "#4ECDC4",
        "transaction_count": 1
      },
      {
        "category_id": 3,
        "category_name": "การขนส่ง",
        "amount": 5000,
        "percentage": 14.3,
        "color": "#45B7D1",
        "transaction_count": 28
      },
      {
        "category_id": 4,
        "category_name": "บันเทิง",
        "amount": 3000,
        "percentage": 8.6,
        "color": "#96CEB4",
        "transaction_count": 12
      }
    ],
    "chart_config": {
      "type": "pie",
      "show_labels": true,
      "show_percentages": true
    }
  }
}
```

**Error Responses**:

**400 Bad Request**:

```json
{
  "success": false,
  "message": "จำนวนเงินขั้นต่ำต้องไม่เป็นค่าลบ",
  "error": "INVALID_MIN_AMOUNT"
}
```

**401 Unauthorized**:

```json
{
  "success": false,
  "message": "ต้องการการยืนยันตัวตน",
  "error": "UNAUTHORIZED"
}
```

### 5. รายงานปิดเดือน

### 7. สรุปการปิดรายเดือน

**GET** `/protected/api/v1/reports/monthly-close`

**คำอธิบาย**: รายงานสรุปสิ้นเดือนที่ครอบคลุมสำหรับการวิเคราะห์การเงิน

**Headers**:

```
Authorization: Bearer {jwt_token}
Content-Type: application/json
```

**Query Parameters**:

| Parameter       | Type    | Required  | คำอธิบาย                                                   | ตัวอย่าง      |
| --------------- | ------- | --------- | ---------------------------------------------------------- | ------------- |
| `year`          | number  | ไม่บังคับ | ปีที่ต้องการดูข้อมูล (ค่าเริ่มต้น: ปีปัจจุบัน)             | 2024          |
| `month`         | number  | ไม่บังคับ | เดือนที่ต้องการดูข้อมูล (1-12, ค่าเริ่มต้น: เดือนปัจจุบัน) | 3             |
| `include_goals` | boolean | ไม่บังคับ | รวมการเปรียบเทียบกับเป้าหมาย (ค่าเริ่มต้น: true)           | true          |
| `user_id`       | string  | ไม่บังคับ | UUID สำหรับ admin เข้าถึงข้อมูลผู้ใช้อื่น                  | "uuid-string" |

**Success Response (200)**:

```json
{
  "success": true,
  "message": "ดึงรายงานสรุปการปิดรายเดือนสำเร็จ",
  "data": {
    "period": "มีนาคม 2024",
    "financial_summary": {
      "total_income": 50000,
      "total_expense": 35000,
      "net_income": 15000,
      "savings_rate": 30.0,
      "expense_ratio": 70.0
    },
    "category_breakdown": {
      "top_expenses": [
        {
          "category": "อาหาร",
          "amount": 15000,
          "percentage": 42.9,
          "transactions": 45
        },
        {
          "category": "ค่าเช่าบ้าน",
          "amount": 12000,
          "percentage": 34.3,
          "transactions": 1
        }
      ],
      "income_sources": [
        {
          "category": "เงินเดือน",
          "amount": 45000,
          "percentage": 90.0
        },
        {
          "category": "รายได้เสริม",
          "amount": 5000,
          "percentage": 10.0
        }
      ]
    },
    "comparison": {
      "vs_previous_month": {
        "income_change": 5.2,
        "expense_change": -2.1,
        "savings_change": 12.5
      },
      "vs_average": {
        "income_vs_avg": 8.7,
        "expense_vs_avg": -5.3
      }
    },
    "goals": {
      "savings_goal": {
        "target": 20000,
        "actual": 15000,
        "achievement": 75.0,
        "status": "below_target"
      },
      "expense_budget": {
        "target": 30000,
        "actual": 35000,
        "overspend": 5000,
        "status": "over_budget"
      }
    },
    "insights": [
      "ค่าใช้จ่ายในหมวดอาหารเพิ่มขึ้น 15% จากเดือนที่แล้ว",
      "การออมเงินอยู่ต่ำกว่าเป้าหมาย 25%",
      "รายได้เพิ่มขึ้นต่อเนื่อง 3 เดือนติดต่อกัน"
    ]
  }
}
```

**Error Responses**:

**400 Bad Request**:

```json
{
  "success": false,
  "message": "เดือนหรือปีไม่ถูกต้อง",
  "error": "INVALID_DATE_PARAMS"
}
```

**401 Unauthorized**:

```json
{
  "success": false,
  "message": "ต้องการการยืนยันตัวตน",
  "error": "UNAUTHORIZED"
}
```

**500 Internal Server Error**:

```json
{
  "success": false,
  "message": "เกิดข้อผิดพลาดภายในเซิร์ฟเวอร์",
  "error": "INTERNAL_ERROR"
}
```

## คุณสมบัติสำหรับผู้ดูแลระบบ

ผู้ดูแลระบบสามารถเข้าถึงรายงานของผู้ใช้คนอื่นได้โดยใส่ parameter `user_id`:

```bash
GET /protected/api/v1/reports/summary?user_id=123&month=2024-03
```

## ตัวอย่างการใช้งาน

### ดูสรุปการเงินเดือนปัจจุบัน

```bash
curl -X GET "http://localhost:3000/protected/api/v1/reports/summary" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### ดูสรุปการเงินเดือนที่กำหนด

```bash
curl -X GET "http://localhost:3000/protected/api/v1/reports/summary?month=2024-03" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### ดูธุรกรรมล่าสุด (จำกัดจำนวน)

```bash
curl -X GET "http://localhost:3000/protected/api/v1/reports/recent-transactions?limit=5" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### การเข้าถึงข้อมูลผู้ใช้ (Admin)

```bash
curl -X GET "http://localhost:3000/protected/api/v1/reports/summary?user_id=123" \
  -H "Authorization: Bearer ADMIN_JWT_TOKEN"
```

## Error Responses

### 400 Bad Request - รูปแบบเดือนไม่ถูกต้อง

```json
{
  "success": false,
  "message": "รูปแบบเดือนไม่ถูกต้อง ต้องเป็น YYYY-MM",
  "data": null
}
```

### 401 Unauthorized

```json
{
  "success": false,
  "message": "จำเป็นต้องมี Authorization token",
  "data": null
}
```

### 500 Internal Server Error

```json
{
  "success": false,
  "message": "ไม่สามารถดึงรายงานสรุปได้",
  "data": null
}
```

## หมายเหตุ

- Parameter วันที่ทั้งหมดต้องอยู่ในรูปแบบ YYYY-MM
- การเข้าถึงแบบ Admin ต้องมีการตรวจสอบ role ที่เหมาะสม (จะดำเนินการในอนาคต)
- ระยะเวลาเริ่มต้นคือเดือนปัจจุบันหากไม่ระบุ
- ค่าเงินทั้งหมดเป็นสกุลเงินหลักของผู้ใช้

---

[← กลับไปยังเอกสาร API](../README.md)
