# 📂 Categories API

## ภาพรวม

จัดการหมวดหมู่รายรับและรายจ่ายสำหรับจัดกลุ่มธุรกรรม

**Base Path**: `/protected/api/v1/categories`

**การยืนยันตัวตน**: จำเป็น (JWT Bearer Token)

## Endpoints

### ดูหมวดหมู่ทั้งหมด

**GET** `/protected/api/v1/categories`

**คำอธิบาย**: ดึงหมวดหมู่ที่มีทั้งหมด

### ดูหมวดหมู่ตาม ID

**GET** `/protected/api/v1/categories/:id`

**คำอธิบาย**: ดูรายละเอียดหมวดหมู่เฉพาะ

### สร้างหมวดหมู่ใหม่

**POST** `/protected/api/v1/categories`

**Request Body**:

```json
{
  "category_name": "ของใช้ในบ้าน"
}
```

### แก้ไขหมวดหมู่

**PATCH** `/protected/api/v1/categories/:id`

**Request Body**:

```json
{
  "category_name": "อาหารและเครื่องดื่ม"
}
```

### ลบหมวดหมู่

**DELETE** `/protected/api/v1/categories/:id`

**คำอธิบาย**: ลบหมวดหมู่ (เฉพาะที่ไม่ได้ใช้ในธุรกรรม)

## รูปแบบ Response

```json
{
  "success": true,
  "message": "ดึงหมวดหมู่สำเร็จ",
  "data": {
    "category_id": 1,
    "category_name": "อาหาร",
    "created_date": "2024-03-15T10:30:00Z"
  }
}
```

---

[← กลับไปยังเอกสาร API](../README.md)
